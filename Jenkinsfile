properties([
  parameters([
    string(name: "mybranch", description: "branch to use"),
  ])
])

pipeline {
  stages {
    stage('build') {
      steps {
        sh 'echo Hello World!'
      }
    }
  }
}
